# Supabase Project Settings -> API. URL und anon/public Key sind fuer den
# Browser bestimmt (NEXT_PUBLIC_*), durch Row Level Security abgesichert --
# kein Secret. Nie den service_role Key hier eintragen, wird nirgends gebraucht.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
