# Secrets — niemals committen
.env
video-use/.env
web/.env.local

# Dependencies
node_modules/
__pycache__/
.venv/
video-use/.venv/
web/node_modules/
web/.next/

# Rohvideos
raw/*.mp4
raw/*.mov
raw/*.mkv
raw/**/*.mp4
raw/**/*.mov
raw/**/*.mkv

# Projekt-Outputs
projects/*/renders/*.mp4
projects/*/previews/*.mp4
projects/*/transcripts/*.json
projects/*/clips/*.mp4
projects/*/_bundle_assets/
projects/*/assets/speaker.mp4

# Caches
.hyperframes-cache/

# OS
.DS_Store
Thumbs.db

# Skill-Junctions -- reproduzierbar per Setup (npm install + Junction-Erstellung),
# kein echter Inhalt, sonst dupliziert Git node_modules/video-use komplett
.claude/skills/

# Lokaler Harness-State, kein Projekt-Inhalt
.claude/scheduled_tasks.lock
